"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.vValidator = void 0;
const validator_1 = require("hono/validator");
const valibot_1 = require("valibot");
const vValidator = (target, schema, hook) => 
// @ts-expect-error not typed well
(0, validator_1.validator)(target, async (value, c) => {
    const result = await (0, valibot_1.safeParseAsync)(schema, value);
    if (hook) {
        const hookResult = hook(result, c);
        if (hookResult instanceof Response || hookResult instanceof Promise) {
            return hookResult;
        }
    }
    if (!result.success) {
        return c.json(result, 400);
    }
    const data = result.output;
    return data;
});
exports.vValidator = vValidator;
