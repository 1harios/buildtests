export function validateFramePostBody(value, c) {
    const buttonIndex = Number.parseInt(value.buttonIndex);
    if (buttonIndex < 1 || buttonIndex > 4)
        return c.text('Invalid data, missing buttonIndex', 400);
    const url = value.url;
    if (!url)
        return c.text('Invalid data, missing url', 400);
    const inputText = value.inputText;
    const state = value.state;
    const fid = value.fid ? Number.parseInt(value.fid) : undefined;
    const castId = value.castId ?? {
        fid: 1,
        hash: '0x0000000000000000000000000000000000000000',
    };
    return {
        buttonIndex,
        castId,
        fid,
        inputText,
        state,
        url,
    };
}
//# sourceMappingURL=validateFramePostBody.js.map