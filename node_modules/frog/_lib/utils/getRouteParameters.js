export function getRouteParameters(...parameters) {
    const options = typeof parameters[parameters.length - 1] === 'object'
        ? parameters[parameters.length - 1]
        : undefined;
    const middlewares = [];
    let handler;
    for (let i = parameters.length - (options ? 2 : 1); i > 0; i--) {
        if (!handler)
            handler = parameters[i];
        else
            middlewares.push(parameters[i]);
    }
    return [parameters[0], middlewares, handler, options];
}
//# sourceMappingURL=getRouteParameters.js.map