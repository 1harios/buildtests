import { loadGoogleFont } from 'hono-og';
export async function parseFonts(fonts) {
    if (!fonts)
        return undefined;
    return await Promise.all(fonts.map(async (font) => {
        if (font.source === 'google')
            return {
                ...font,
                data: await loadGoogleFont({
                    family: font.name,
                    weight: font.weight,
                    style: font.style,
                }),
            };
        return font;
    }));
}
//# sourceMappingURL=parseFonts.js.map