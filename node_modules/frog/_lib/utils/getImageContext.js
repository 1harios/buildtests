export function getImageContext(parameters) {
    const { context } = parameters;
    const { env, req } = context || {};
    return {
        context: {
            env,
            req,
            res: (data) => ({ data, format: 'image', status: 'success' }),
            var: context.var,
        },
    };
}
//# sourceMappingURL=getImageContext.js.map