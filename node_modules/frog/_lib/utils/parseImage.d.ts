import type { Frog } from '../frog.js';
import type { Child, Direction } from '../ui/types.js';
export declare function parseImage(node_: Child, options: {
    assetsUrl: string;
    ui: Frog['ui'] & {
        direction?: Direction | undefined;
    };
}): Promise<Child>;
//# sourceMappingURL=parseImage.d.ts.map