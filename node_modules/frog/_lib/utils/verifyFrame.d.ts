import { Message } from '../protobufs/generated/message_pb.js';
import type { FrameData, TrustedData } from '../types/frame.js';
import type { Hub } from '../types/hub.js';
export type VerifyFrameParameters = {
    frameUrl: string;
    hub: Hub;
    trustedData: TrustedData;
    url: string;
};
export type VerifyFrameReturnType = {
    frameData: FrameData;
};
export declare function verifyFrame({ frameUrl, hub, trustedData, url, }: VerifyFrameParameters): Promise<VerifyFrameReturnType>;
export declare function messageToFrameData(message: Message): FrameData;
//# sourceMappingURL=verifyFrame.d.ts.map